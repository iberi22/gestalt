[build]
# NOTE: target-dir removed — machine-local RAM tmpfs optimization (skill rust-build-ramdisk)
# broke CI runners (/build/rust-target Permission denied on GitHub Actions).

[alias]
check-fast = "check -p gestalt_timeline --no-default-features"
check-full = "check -p gestalt_timeline --features telegram,rag-embeddings"
